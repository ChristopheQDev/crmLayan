{% extends 'base.html.twig' %}

{% block title %}Fiche entreprise{% endblock %}

{% block body %}
    <h1>Fiche entreprise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ company.id }}</td>
            </tr>
            <tr>
                <th>Nom entreprise</th>
                <td>{{ company.companyName }}</td>
            </tr>
            <tr>
                <th>Siren</th>
                <td>{{ company.siren }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('company_index') }}">REVENIR A LA LISTE</a>

    <a href="{{ path('company_edit', {'id': company.id}) }}">MODIFIER</a>

    {{ include('company/_delete_form.html.twig') }}
{% endblock %}
